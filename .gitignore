# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Python cache
__pycache__/
*.pyc

# Reference screenshots of other systems — notes, not part of the app,
# and 4.8MB the repository does not need to carry.
Masters/

# Data backups. These hold real customer, order and staff records, and this
# repository is public — a backup committed once stays in the history.
backup-*.json
